spring:
  main:
    keep-alive: true
  application:
    name: dontcare
  jpa:
    hibernate:
      generate-ddl: true
      ddl-auto: update
  flyway:
    baseline-on-migrate: true
    baseline-version: '0'

logging:
  level:
    it.gov.acn: DEBUG